# NextAuth (obligatoire)
AUTH_SECRET=          # Générer avec: npx auth secret

# Google OAuth (auth Google)
AUTH_GOOGLE_ID=       # Client ID depuis Google Cloud Console
AUTH_GOOGLE_SECRET=   # Client Secret depuis Google Cloud Console

# Vercel KV (Redis) - fournis par Vercel quand tu connectes un KV store
KV_REST_API_URL=      # URL REST du store KV
KV_REST_API_TOKEN=    # Token d'accès

# Optionnel: URL de l'app (auto en prod sur Vercel)
# AUTH_URL=http://localhost:3000

# Optionnel: mode dev avec faux users (à ne pas activer en prod)
# ALLOW_DEV_CREDENTIALS=true
# NEXT_PUBLIC_DEV_CREDENTIALS=true
# → Affiche un formulaire "Connexion dev" sur /auth/signin (users: dev-alice, dev-bob, dev-charlie / mot de passe: dev)
